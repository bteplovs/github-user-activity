# github user activity

A CLI tool to see a given user's github activity via the Github API.

## Usage

Clone the repo and navigate to github-user-activity

then run:
```
dotnet run --  <git-username>  
```
